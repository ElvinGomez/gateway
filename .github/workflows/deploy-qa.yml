name: Build QA release

on:
  push:
    branches: [qa]

jobs:
  prepare-release:
    runs-on: [self-hosted, mac-mini-qa]
    steps:
      - run: bash /Users/elvingomez/Documents/GitHub/infra/infra/qa/scripts/prepare-release.sh gateway "$GITHUB_SHA"
