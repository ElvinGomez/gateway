{
  "name": "tripsi-api-gateway",
  "version": "1.0.0",
  "private": true,
  "description": "Vercel API gateway: routes in vercel.json with ${ORIGIN} from env",
  "scripts": {
    "build": "tsc",
    "vercel-build": "tsc"
  },
  "dependencies": {
    "@vercel/node": "^3.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
